extends: default

ignore: |
  .venv/
  **/target/
  **/dbt_packages/
  **/logs/

rules:
  line-length:
    max: 200
  truthy: disable
  document-start: disable
  empty-lines:
    max: 1
  new-line-at-end-of-file: enable
