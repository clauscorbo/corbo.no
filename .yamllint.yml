extends: default

ignore: |
  .venv/
  **/target/
  **/dbt_packages/
  **/logs/

rules:
  line-length: disable
  truthy: disable
  document-start: disable
  empty-lines:
    max: 1
  new-line-at-end-of-file: enable
